# Security

This repository is a public product showcase.

Do not commit:

- passwords, API keys, access tokens, session secrets;
- private keys or certificates;
- internal IP addresses or private network topology;
- production environment files;
- database credentials;
- private user data;
- raw private memory or conversation datasets.

If a secret is accidentally committed, rotate it immediately rather than relying on deletion from Git history.
